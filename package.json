{
  "name": "mates",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "cd client && npm install",
    "server-install": "cd server && npm install",
    "build": "npm-run-all --parallel client-install server-install",
    "dev": "npm run dev --prefix server & npm run dev --prefix client",
    "client": "",
    "server": ""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}